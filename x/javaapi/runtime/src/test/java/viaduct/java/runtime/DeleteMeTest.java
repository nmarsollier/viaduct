package viaduct.java.runtime;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

/** Test for DeleteMe placeholder class. TODO: Delete this file once real tests are in place. */
class DeleteMeTest {
  @Test
  void testMeaning() {
    DeleteMe instance = new DeleteMe();
    assertEquals(42, instance.meaning());
  }
}
